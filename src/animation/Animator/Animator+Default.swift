import Foundation
/**
 * Defaults
 */
extension Animator {
   public static let initValues: InitValues = (dur: 0.5, from: 0, to: 1) // Default init values
   static let fps: CGFloat = 60 // Fixme: ⚠️️ this should be derived from a device variable
}
